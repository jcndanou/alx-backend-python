apiVersion: v1
kind: Service
metadata:
  name: messaging-app-service
spec:
  selector:
    app: messaging-app
    # Start with selector pointing to the blue version
    version: blue
  ports:
    - protocol: TCP
      port: 8000
      targetPort: 8000